#!/bin/bash
if [ $# -lt 1 ]; then
	echo "repeat  --  Repeats a given command (ls / ps / etc.) every second"
	echo "Usage: repeat {CommandToRepeat}"
	exit 1
fi

while [ 1 == 1 ]; do
	$*
	sleep 1

done
